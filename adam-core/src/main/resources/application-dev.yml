spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    database: 0
    timeout: 10000
  rabbitmq:
    host: localhost
    port: 5672
    virtual-host: /
    username: guest
    password: guest
  datasource:
    druid:
      name: adam-test
      url: jdbc:mysql://localhost:3306/almas?useSSL=false&useUnicode=true&characterEncoding=utf-8&autoReconnect=true&serverTimezone=Asia/Shanghai
      username: root
      password: root
#      validationQuery: SELECT 1 FROM DUAL
#      filters: [stat,wall,log4j]
#      useGlobalDataSourceStat: true
#      test-while-idle: false
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
      web-stat-filter:
        enabled: true
        url-pattern: /*


mybatis-plus:
  typeAliasesPackage: com.yyovo.adam.**.model.pojo
  typeEnumsPackage: com.yyovo.adam.**.model.enums
  #  mapperLocations:/
  #  mapperLocations: classpath*:/mapper/**/*.xml
  configuration:
    #自动驼峰命名规则
    map-underscore-to-camel-case: true
  global-config:
    superMapperClass: com.yyovo.adam.common.base.dao.SuperMapper
    db-config:
      table-prefix: y_
